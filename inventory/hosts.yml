---
all:
  vars:
    ansible_user: root
    ansible_port: 22

  hosts:
    # Add your hosts here:
    # web01:
    #   ansible_host: 192.168.1.10
    #   ansible_user: ubuntu
    # db01:
    #   ansible_host: 192.168.1.11

  children:
    linux:
      hosts: {}

    workstations:
      hosts: {}

    servers:
      hosts: {}

    containers:
      hosts: {}

    databases:
      hosts: {}
